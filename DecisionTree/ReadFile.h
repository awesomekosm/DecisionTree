#pragma once
#include <fstream>
#include <iostream>
#include <vector>
#include <string>

class ReadFile
{
public:
    ReadFile();
    std::string ReadFile::readFile(const char *);
    ~ReadFile();
};

